<UserControl x:Class="Labb03_GUI.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb03_GUI.Views"
             mc:Ignorable="d" 
             d:Background="PapayaWhip"
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border 
            BorderBrush="Gray"
            BorderThickness="2"
            HorizontalAlignment="Left"
            VerticalAlignment="Top">
            <StackPanel
                Orientation="Vertical"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Cornsilk">
                <TextBlock
                    FontSize="10"
                    Margin="0 3 0 0"
                    Text="p o i n t s"
                    Foreground="Red"
                    HorizontalAlignment="Center"
                        />
                <StackPanel 
                    Orientation="Horizontal"                
                    >
                    <TextBlock
                        FontSize="30"
                        Margin="15 0 0 10"
                        Text="{Binding NumberOfCorrectAnswers}"
                            />
                    <TextBlock
                        FontSize="30"
                        Margin="0 0 0 10"
                        Text="/"
                            />
                    <TextBlock
                        FontSize="30"
                        Margin="0 0 15 10"
                        Text="{Binding ActivePack.Questions.Count}"
                            />
                </StackPanel>
            </StackPanel>
        </Border>
        <StackPanel
            Orientation="Vertical"
            HorizontalAlignment="Center"
            VerticalAlignment="Top">
            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Center">
                <TextBlock
                    FontSize="40"
                    Margin="15 15 0 15"
                    Foreground="Purple"
                    Text="Question "
                        />
                <TextBlock
                    FontSize="40"
                    Margin="0 15 15 15"
                    Foreground="Purple"
                    Text="{Binding NumberOfCurrentQuestion}"
                        />
            </StackPanel>
            <TextBlock
                Margin="50 10 50 0"
                FontSize="27"
                Text="{Binding CurrentQuestion.Query}"
                TextWrapping="Wrap"
                TextAlignment="Center"
                />
        </StackPanel>
            
        <TextBlock
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Text="{Binding TimeLeft}" 
            Margin="15"
            FontSize="25"/>
        <ItemsControl 
            ItemsSource="{Binding AnswerViewModels}"
            Grid.Row="1"
            Grid.RowSpan="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="2" Columns="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Text}" 
                        Command="{Binding DataContext.CheckAnswerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Cornsilk"/>
                                <Setter Property="BorderBrush" Value="Gray"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Margin" Value="5"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCorrect}" Value="True">
                                        <Setter Property="Background" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsCorrect}" Value="False">
                                        <Setter Property="Background" Value="Red"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="Blue"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                        
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
    </Grid>

</UserControl>
